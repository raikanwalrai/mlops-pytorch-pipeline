apiVersion: v1
kind: Service

metadata:
  name: pytorch-serving-service
  namespace: mlops-pipeline

spec:
  type: ClusterIP

  selector:
    app: pytorch-serving

  ports:

  - port: 80
    targetPort: 8080
    protocol: TCP